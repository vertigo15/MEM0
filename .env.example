# ==============================================================================
# Mem0 Application Environment Configuration
# ==============================================================================
# Copy this file to .env and fill in your actual values

# ==============================================================================
# LLM PROVIDER CONFIGURATION (Required)
# ==============================================================================
LLM_PROVIDER=azure_openai

# Azure OpenAI - GPT-4o Configuration (for memory processing)
LLM_AZURE_ENDPOINT=https://your-resource.openai.azure.com/
LLM_AZURE_OPENAI_API_KEY=your-azure-openai-api-key
LLM_AZURE_DEPLOYMENT=your-gpt-4o-deployment-name
LLM_AZURE_API_VERSION=2024-12-01-preview

# Azure OpenAI - Embedding Configuration (for semantic search)
EMBEDDING_AZURE_ENDPOINT=https://your-resource.openai.azure.com/
EMBEDDING_AZURE_OPENAI_API_KEY=your-azure-openai-api-key
EMBEDDING_AZURE_DEPLOYMENT=text-embedding-ada-002
EMBEDDING_AZURE_API_VERSION=2023-05-15

# ==============================================================================
# DATABASE CONFIGURATION (Optional - using Qdrant by default)
# ==============================================================================
# Uncomment to use PostgreSQL instead of Qdrant
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_USER=postgres
# DATABASE_PASSWORD=postgres123
# DATABASE_NAME=mem0_db

# ==============================================================================
# APPLICATION CONFIGURATION (Optional)
# ==============================================================================
APP_PORT=8000
LOG_LEVEL=INFO
WORKERS=1

# ==============================================================================
# OPTIONAL STORAGE CONFIGURATION
# ==============================================================================
# Uncomment if you want to use Azure Blob Storage for file attachments
# STORAGE_PROVIDER=azure_blob
# AZURE_STORAGE_ACCOUNT_NAME=your-storage-account
# AZURE_STORAGE_ACCOUNT_KEY=your-storage-key
# AZURE_STORAGE_CONTAINER_NAME=mem0-container

# ==============================================================================
# SETUP INSTRUCTIONS
# ==============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Update your Azure OpenAI resource endpoint (replace 'your-resource')
# 3. Add your Azure OpenAI API key
# 4. Set your GPT-4o deployment name
# 5. Verify text-embedding-ada-002 deployment name
# 6. Run: python -m uvicorn app:app --host 0.0.0.0 --port 8000
#
# Note: This configuration uses Qdrant (local file storage) for vectors.
# For production, uncomment the DATABASE_* variables to use PostgreSQL.
